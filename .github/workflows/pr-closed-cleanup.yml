name: PR Closed Cleanup

on:
    pull_request:
        types: [closed]

jobs:
    cleanup:
        permissions:
            # Required to cancel queued and in-progress runs for the closed pull request.
            actions: write
        uses: rnw-community/mobile-ci/.github/workflows/pr-closed-cleanup-reusable.yml@0d82cf6287e64151cb16859732929cf39a0910dc # v1.4.2
