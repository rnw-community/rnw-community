name: PR Closed Cleanup

on:
    pull_request:
        types: [closed]

jobs:
    cleanup:
        permissions:
            # Required to cancel queued and in-progress runs for the closed pull request.
            actions: write
        uses: rnw-community/mobile-ci/.github/workflows/pr-closed-cleanup-reusable.yml@054f68edd9440db7997f964cc17335d3cfcebce4 # v1.4.0
