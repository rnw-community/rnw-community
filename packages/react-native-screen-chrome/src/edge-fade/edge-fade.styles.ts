import { StyleSheet } from 'react-native';

export const edgeFadeStyles = StyleSheet.create({
    band: {
        position: 'absolute',
        left: 0,
        right: 0,
        zIndex: 2,
    },
    fill: {
        flex: 1,
    },
});
