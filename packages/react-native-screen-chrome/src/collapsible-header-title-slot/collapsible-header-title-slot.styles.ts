import { StyleSheet } from 'react-native';

export const collapsibleHeaderTitleSlotStyles = StyleSheet.create({
    slot: {
        flex: 1,
        position: 'relative',
        zIndex: 1,
    },
});
