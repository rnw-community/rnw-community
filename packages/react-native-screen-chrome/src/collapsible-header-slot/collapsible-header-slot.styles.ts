import { StyleSheet } from 'react-native';

export const collapsibleHeaderSlotStyles = StyleSheet.create({
    slot: {
        minWidth: 44,
        minHeight: 44,
        alignItems: 'center',
        justifyContent: 'center',
        zIndex: 1,
    },
});
