/**
 * One entry of the scrollable content every demo screen renders.
 */
export interface ChromeDemoItemInterface {
    readonly detail: string;
    readonly id: number;
    readonly title: string;
}
