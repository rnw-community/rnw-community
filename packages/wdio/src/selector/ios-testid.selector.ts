export const iosTestIDSelector = (testID: string): string => `~${testID}`;

/*
 * TODO: This version is for future
 * export const iOSTestID = (testID: string): string => `-ios predicate string:label == '${testID$}'`;
 */
