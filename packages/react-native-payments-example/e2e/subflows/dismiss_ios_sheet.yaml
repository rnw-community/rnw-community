appId: ${APP_ID}
---
- runFlow:
    when:
      platform: iOS
    commands:
      - extendedWaitUntil:
          visible:
            id: "dismiss"
          timeout: 15000
      - tapOn:
          id: "dismiss"
      - extendedWaitUntil:
          visible:
            id: "payments-flow-state"
            text: "rejected"
          timeout: 20000
